/*
 * @group integration
 */
describe("Vehicle Api Endpoint Test Suite", () => {
  describe("GET /vehicles/:id", () => {
    it("should return status 200 and vehicle json response", () => {
      expect(true).toBe(false);
    });
  });
});
